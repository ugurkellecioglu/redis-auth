// i'll use this type in the register as well
// ideally we should have a confirm password field in the register form
// but in this demo we'll keep it simple
export interface LoginInput {
  email: string
  password: string
}
